PORT=4000
REPO_DIR=/path/to/your/repo          # default: process.cwd()
GIT_PUSH=false                       # set true once remote is configured

# Immich
IMMICH_URL=https://immich.your-domain.tld
IMMICH_API_KEYS=KEY_FOR_USER_A,KEY_FOR_USER_B  # supports multiple editors

# (optional) track service if you add it later:
DAWARICH_URL=
DAWARICH_TOKEN=
